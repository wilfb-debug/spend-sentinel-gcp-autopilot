name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            Services-detectors/inventory-service/requirements.txt
            requirements-dev.txt

      - name: Install dependencies
        run: |
          pip install -r Services-detectors/inventory-service/requirements.txt
          pip install -r requirements-dev.txt

      - name: Lint (flake8)
        run: |
          flake8 Services-detectors/ --max-line-length=100

      - name: Test (pytest)
        run: |
          python -m pytest tests/ -v --cov=Services-detectors/inventory-service --cov-report=term-missing --cov-fail-under=80

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: lint-and-test

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t spend-sentinel:ci ./Services-detectors/inventory-service
